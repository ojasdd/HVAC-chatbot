# Core parsing
unstructured[pdf]>=0.13.0
pymupdf>=1.24.0

# Chunking / tokenisation
tiktoken>=0.7.0

# Embeddings
sentence-transformers>=3.0.0
torch>=2.2.0              # CPU build is fine; install GPU variant separately

# Vector DB
chromadb>=0.5.0

# Utilities
pillow>=10.0.0
pandas>=2.0.0

# Web Backend
fastapi
uvicorn
sse-starlette
